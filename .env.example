# Copy this file to .env if you use a local server that reads environment variables.
# For the default static-file setup, paste your key directly into config.js instead.
ANTHROPIC_API_KEY=your_key_here
